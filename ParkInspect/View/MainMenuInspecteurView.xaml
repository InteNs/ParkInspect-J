<UserControl x:Class="ParkInspect.View.MainMenuInspecteurView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ParkInspect"
        xmlns:view="clr-namespace:ParkInspect.View"
        mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Button Grid.Row="0" Command="{Binding SetViewCommand}" CommandParameter="Customers-list">Inspectie uitvoeren</Button>
        <Button Grid.Row="1" Command="{Binding SetViewCommand}" CommandParameter="Customers-list">Inspectie inplannen</Button>
        <Button Grid.Row="2" Command="{Binding SetViewCommand}" CommandParameter="templates-list">Templatescherm</Button>
        <Button Grid.Row="3" Command="{Binding SetViewCommand}" CommandParameter="questions-list">Vragenscherm</Button>
        <Button Grid.Row="4" Command="{Binding SetViewCommand}" CommandParameter="employees-list">Rapport genereren</Button>
    </Grid>
</UserControl>